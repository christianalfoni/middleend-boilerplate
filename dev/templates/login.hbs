<html>
<head>
    <title>{{title}}</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style>

        html, body {
            height: 100%;
        }

        body {
            font-size: 100%;
            font-family: Arial;
            color: #333;
            -webkit-font-smoothing: antialiased;
            -moz-font-smoothing: antialiased;
            -ms-font-smoothing: antialiased;
            -o-font-smoothing: antialiased;
            font-smoothing: antialiased;
            background: #e4e4e4; /* Old browsers */
            background: -moz-linear-gradient(top, #e4e4e4 0%, #c4c4c4 100%); /* FF3.6+ */
            background: -webkit-gradient(linear, left top, left bottom, color-stop(0%, #e4e4e4), color-stop(100%, #c4c4c4)); /* Chrome,Safari4+ */
            background: -webkit-linear-gradient(top, #e4e4e4 0%, #c4c4c4 100%); /* Chrome10+,Safari5.1+ */
            background: -o-linear-gradient(top, #e4e4e4 0%, #c4c4c4 100%); /* Opera 11.10+ */
            background: -ms-linear-gradient(top, #e4e4e4 0%, #c4c4c4 100%); /* IE10+ */
            background: linear-gradient(to bottom, #e4e4e4 0%, #c4c4c4 100%); /* W3C */
            text-align: center;
            text-shadow: 0px 1px 0px rgba(255, 255, 255, 1);
        }

        h1 {
            font-family: Gill Sans;
            font-size: 3em;
        }

        #center {
            width: 960px;
            display: inline-block;
            margin: 0 auto;
            margin-top: 10%;
        }

        input, button {
            width: 50%;
            height: 48px;
            border-radius: 5px;
            border: 1px solid #7a7a7a;
            margin-bottom: 12px;
            text-indent: 12px;
            font-family: Arial, Consolas;
            font-size: 1.2em;
            outline: none;
            font-weight: normal;
            -webkit-box-shadow: 0px 1px 0px rgba(255, 255, 255, 1);
            -moz-box-shadow:    0px 1px 0px rgba(255, 255, 255, 1);
            box-shadow:         0px 1px 0px rgba(255, 255, 255, 1);

        }

        button {
            width: 20%;
            text-align: center;
            background: #98de00; /* Old browsers */
            background: -moz-linear-gradient(top, #98de00 0%, #86c400 100%); /* FF3.6+ */
            background: -webkit-gradient(linear, left top, left bottom, color-stop(0%, #98de00), color-stop(100%, #86c400)); /* Chrome,Safari4+ */
            background: -webkit-linear-gradient(top, #98de00 0%, #86c400 100%); /* Chrome10+,Safari5.1+ */
            background: -o-linear-gradient(top, #98de00 0%, #86c400 100%); /* Opera 11.10+ */
            background: -ms-linear-gradient(top, #98de00 0%, #86c400 100%); /* IE10+ */
            background: linear-gradient(to bottom, #98de00 0%, #86c400 100%); /* W3C */
            color: #FFF;
            text-shadow: 0px 0px 2px rgba(0, 0, 0, 1);
            font-weight: bold;
            -webkit-font-smoothing: antialiased;
            -moz-font-smoothing: antialiased;
            -ms-font-smoothing: antialiased;
            -o-font-smoothing: antialiased;
            font-smoothing: antialiased;
            font-size: 1.4em;
            line-height: 1.4em;
        }
    </style>
</head>
<body>
<div id="center">
    <h1>Marcello Middleend Boilerplate</h1>

    <form action="/login" method="POST">
        <input name="login" placeholder="Enter username..." autocomplete="off"/>
        <input type="password" name="password" placeholder="Enter password..."/>
        <p>
            <button type="submit">login</button>
        </p>

    </form>

    <script>
        document.getElementsByTagName('input')[0].focus();
    </script>
</div>
</body>
</html>
